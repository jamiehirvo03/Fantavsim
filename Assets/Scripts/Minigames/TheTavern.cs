using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TheTavern : MonoBehaviour
{
    //Handles movement

    //Tells minigame manager which minigame to start

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
